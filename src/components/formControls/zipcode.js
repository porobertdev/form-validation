/* eslint-disable import/prefer-default-export */
import Input from '../input';

export const zipcode = Input({
    label: {
        id: 'zipcode',
        text: 'Zip Code:',
    },
    type: 'text',
});
